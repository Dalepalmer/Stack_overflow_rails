<%= form_for [@question, @answer], remote: true do |f| %>
  <%= f.text_area :body, :placeholder => 'Your answer here', :rows => '6' %>
  <%= f.submit "Submit the winning answer", :class => "btn btn-success" %>
<% end %>
